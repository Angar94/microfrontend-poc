name: Micro Apps deploy

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup node
      uses: actions/setup-node@v4
      with:
        node-version: latest

    - name: Install
      run: npm install

    - name: Build                                                                         
      run: npm run build

    - id: 'auth'
      uses: 'google-github-actions/auth@v2'
      with:
        workload_identity_provider: 'principal://iam.googleapis.com/projects/720030352680/locations/global/workloadIdentityPools/my-pool/subject/SUBJECT_ATTRIBUTE_VALUE'
        # ${{ secrets.WIF_PROVIDER }}
        service_account: 'micro-app@microapp-poc.iam.gserviceaccount.com'
        # ${{ secrets.GCP_SA }}

    - id: 'upload-folder'
      uses: 'google-github-actions/upload-cloud-storage@v2'
      with:
        path: '/dist'
        destination: 'micro-app-claims'

    # Example of using the output
    # - id: 'uploaded-files'
    #   uses: 'foo/bar@v1'
    #   env:
    #     files: '${{ steps.upload-folder.outputs.uploaded }}'